# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

두 축이 대등하게 핵심이다. 어느 한쪽을 위해 다른 쪽을 희생하지 않는다.

- **기관 관리자** (센터장·사무국장·사회복지사) — 이 저장소의 관리자 웹을 쓴다. 데스크톱에서 근무표를 짜고, 휴무를 승인하고, 결재를 처리하고, 배차를 조정한다. 한 화면에서 여러 사실을 동시에 확인하며 일한다.
- **현장 직원** (요양보호사·간호조무사·사회복지사) — 별도 저장소의 Flutter 앱(`frontend-app`)과 웹 직원 화면(`/employee`)을 쓴다. 이동 중이거나 근무 사이 짬에 폰으로 휴무를 신청하고, 공지와 근무를 확인한다.

## Product Purpose

주간보호센터와 장기요양기관의 근무표를 만들고 지키게 한다. 휴무 신청부터 승인, 근무표 반영, 현장 공지까지가 한 곳에서 이어지는 것이 성공이다. 엑셀 파일과 카카오톡 대화로 흩어져 있던 과정을 하나의 기록으로 모은다.

## Positioning

경쟁 제품이 그대로 따라 하기 어려운 지점:

- **근무표와 휴무가 같은 화면에 있다.** 휴무 신청이 곧 근무표 변경으로 이어지므로, 승인하는 사람이 "이 사람이 빠지면 그날 누가 남는가"를 같은 자리에서 본다.
- **기관 운영 올인원.** 근무표에서 끝나지 않고 전자결재·배차관리·공지·자료실·고충함까지 한 제품에 있다. 기관이 도구를 여러 개 쓰지 않아도 된다.
- **장기요양 현장의 규칙을 그대로 반영한다.** 날짜별 최소 배치 인원, 직종별 휴무 한도, 차량 노선의 주·부운전자 대체 같은 규칙이 일반 근태 도구에는 없다.
- **직원 앱이 현장에 붙어 있다.** 공지·채팅·휴무 신청이 폰으로 바로 가므로, 관리자가 만든 결정이 현장까지 도달한다.

## Operating Context

- 관리자는 **데스크톱 브라우저**에서 하루 종일 열어두고 쓴다. 월간일정·근무조정 화면이 주 작업면이다.
- 직원은 **모바일**이 기본이다. 같은 백엔드(`silverithm.site`)를 관리자 웹과 공유한다.
- 휴무는 **매월 마감일 전에 몰아서** 신청된다. 마감 직전 신청이 집중되고, 관리자는 그때 한꺼번에 승인·조정한다.
- 배차 설정(노선·주부운전자·어르신 탑승)은 **회사당 한 벌**이고 서버가 원본이다. 관리자 웹과 직원 앱이 같은 데이터를 본다.
- 기관은 **구독제**로 쓴다. 정식 가입 전 체험 모드로 예시 데이터를 둘러볼 수 있다.

## Capabilities and Constraints

**확인된 기능**

근무조정(휴무 신청·승인·한도·마감일), 월간일정(일정과 휴무를 한 달력에서, 담당 업무 체크), 전자결재(양식·결재선·HWP 문서), 공지사항, 실시간 채팅(STOMP), 회원관리, 배차관리(노선·운전자·어르신 탑승·결석), 자료실, 고충·건의함, 케어브이 커뮤니티, AI 글쓰기, 구독·결제.

**제약**

- **화면 밀도를 유지한다.** 관리자는 한 화면에서 많은 정보를 본다. 여백을 넓혀 스크롤을 늘리는 방향의 변경은 이 제품에서 개선이 아니다. 타이포 base가 13px인 것도 같은 이유다.
- 다크모드는 현재 꺼져 있다(`color-scheme: light` 고정). 토큰은 `light-dark()`를 쓰지만 라이트값으로만 해석된다.
- 접근성은 일반적인 웹 수준을 지킨다. 이 제품만의 추가 요구는 확인된 바 없다.

## Brand Commitments

- 이름은 **케어브이(CareV)**.
- 브랜드 색은 **틸**이며 토큰(`--color-accent`, `--color-icon-teal`)으로만 쓴다. 저장소 규약(`CLAUDE.md`)에 하드코딩 금지가 명시돼 있고, `#0d9488`은 대비 3.0:1로 기각된 값이라 쓰지 않는다.
- 본문 서체는 **Figtree**(`--font-figtree`).
- 디자인 시스템은 **Astryx**(`@astryxdesign/core`). Tailwind와 Mantine은 제거됐다.

## Evidence on Hand

- 동작하는 제품 전체가 이 저장소에 있다(`src/app`, `src/components`).
- 체험 모드가 실제로 동작한다 — 예시 기관 데이터로 전 기능을 둘러볼 수 있다.
- SEO용 블로그 글과 FAQ가 `src/lib/blogPosts.ts`, `src/lib/faqData.ts`에 실려 있다.
- 실제 고객사 사례·후기·이용 기관 수는 확인되지 않았다. **지어내지 말 것.**

## Product Principles

1. **결정과 그 결과를 같은 화면에서 보게 한다.** 휴무를 승인할 때 그날 남는 인원이, 배차를 짤 때 주운전자의 휴무가 함께 보여야 한다.
2. **관리자의 밀도와 직원의 단순함은 다른 문제다.** 한 화면을 두 사용자에게 그대로 물려주지 않는다.
3. **현장 규칙을 소프트웨어가 먼저 안다.** 최소 인원, 직종 한도, 주·부운전자 대체 같은 규칙을 사람이 기억하게 만들지 않는다.
4. **기록이 흩어지지 않게 한다.** 한 곳에서 정한 것이 다른 화면·다른 기기에서도 같은 값으로 보인다.
5. **장식보다 읽히는 것이 먼저다.** 관리자는 하루 종일 이 화면을 본다.
